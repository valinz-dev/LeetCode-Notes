Problem: Container With Most Water
Difficulty: Medium

Restatement:
Choose two lines to maximize area.

Approach (Two Pointers):
- i=0, j=n-1.
- area = min(h[i], h[j])*(j-i).
- Move shorter line inward.

Python:
def maxArea(height):
    i, j = 0, len(height)-1
    best = 0
    while i < j:
        best = max(best, min(height[i], height[j])*(j-i))
        if height[i] < height[j]:
            i += 1
        else:
            j -= 1
    return best

Notes:
- O(n), O(1).
